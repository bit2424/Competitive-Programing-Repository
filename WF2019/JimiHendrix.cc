#include <bits/stdc++.h>
#define pb push_back
#define mp make_pair
#define fst first
#define snd second
#define all(cont) cont.begin, cont.end
#define foreach(it, l) for(auto it = l.begin(); it != l.end; it++)
#define fore(i,a,b) for(ll i = a, almo5t = b; i<almo5t; ++i)
#define SZ(x) ((int)x.size())
#define EPS 1e-9
#define FIN std::ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)
#define ENDL "\n"
typedef long long int ll;
using namespace std;
/*
    Petrozavodsk  Winter  Training  Camp  2016 A
    Pr1:
    We have a tree with letter in its edges 
    we want to find if we can make a string 
    from a path between 2 vertex such that
    a string w is a   

    Aproach 1:
    What if we try and look for all the edges that contain
    The letters we are interested in.

    Then we should have a group of pairs of nodes that we could use to form the path.


    Una especie de DP 


*/

void solve(){
    int n;cin>>n;
    
}

int main(){
    FIN;
    //DONT FORGET TO ERASE THIS SHIT XD
    freopen("../in.txt", "r", stdin);freopen("../out.txt", "w", stdout);
    int t=1;
    cin>>t;
    while(t--){
        solve();
    }
    //cout<<flush;
}
