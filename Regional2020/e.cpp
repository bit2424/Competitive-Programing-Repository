#include <bits/stdc++.h>
#define pb push_back
#define mp make_pair
#define fst first
#define snd second
#define all(cont) cont.begin, cont.end
#define foreach(it, l) for(auto it = l.begin(); it != l.end; it++)
#define fore(i,a,b) for(ll i = a, almo5t = b; i<almo5t; ++i)
#define SZ(x) ((int)x.size())
#define EPS 1e-9
#define FIN std::ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)
#define ENDL "\n"
typedef long long int ll;
using namespace std;

int N = 1000005;


/*
    Prob1: Tenemos  una serie de numeros y queremos saber
    para cada numero cuantos rangos cumplen la condición de que
    ningun elemento en el rango sea mayor al elemento que este en el borde
    (Sin contar el numero que estoy evaluando)

    hint1: sería buena idea preguntar cual es el numero más grande a la izquierda
    y a la derecha.
    En esos rasgos contar de numeros en ese rango, que sean mayores al valor
    que estoy evaluando. 
    

*/
void solve(){
    
}

int main(){
    FIN;
    //DONT FORGET TO ERASE THIS SHIT XD
    freopen("../in.txt", "r", stdin);freopen("../out.txt", "w", stdout);
    int t = 1;
    //cin>>t;
    while(t--){
        solve();
    }
}

/*
Notes:

*/  