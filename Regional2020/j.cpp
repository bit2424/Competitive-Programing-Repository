#include <bits/stdc++.h>
#define pb push_back
#define mp make_pair
#define fst first
#define snd second
#define all(cont) cont.begin, cont.end
#define foreach(it, l) for(auto it = l.begin(); it != l.end; it++)
#define fore(i,a,b) for(ll i = a, almo5t = b; i<almo5t; ++i)
#define SZ(x) ((int)x.size())
#define EPS 1e-9
#define FIN std::ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)
#define ENDL "\n"
typedef long long int ll;
using namespace std;

int N = 1000005;


/*
    Problema: Tenemos una matriz con x filas y 8 columnas
    queremos ver en un momento dado cuantas de esas filas cumplen el
    requerimiento de tener valores a mayores a cierta fila proveida

    k<=8


    1 2 3 
    2 2 2
    3 2 1
    -1 -1 -1
    1 2 2
    3 3 3 

    
    3 3 3

    2 3 2

    hint1: La cantidad de computadores disponibles puede ser muy grande.
    hint2: Puedo tener sets o priorities ques 

*/
void solve(){
    
}

int main(){
    FIN;
    //DONT FORGET TO ERASE THIS SHIT XD
    freopen("../in.txt", "r", stdin);freopen("../out.txt", "w", stdout);
    int t = 1;
    //cin>>t;
    while(t--){
        solve();
    }
}

/*
Notes:

*/  